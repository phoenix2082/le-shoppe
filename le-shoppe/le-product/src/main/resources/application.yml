eureka:
  client:
    healthcheck:
        enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
server:
    port: 8086

spring:
    application:
        name: "le-product"
    cloud:
        config:
            enabled: false